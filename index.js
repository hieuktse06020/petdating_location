/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';
import a from './screens/nearBy'
AppRegistry.registerComponent(appName, () => a);
